<h1><%= @mechanic.name %></h1>

<p>Years of Experience: <%= @mechanic.years_experience %></p>

<div id="mechanic_rides">
  <h2>Current rides they're working on:</h2>
  <% @mechanic.rides.each do |ride| %>
    <p><%= ride.name %></p>
  <% end %>
</div>

<div id="add_ride_to_mechanic">
  <h2>Add a ride to workload:</h2>
  <%= form_with url: mechanic_rides_path(@mechanic), data: {turbo: :false} do |f| %>
    <%= f.label :ride_id, 'Ride Id:' %>
    <%= f.number_field :ride_id %>
    <%= f.submit "Submit" %>
  <% end %>
</div>